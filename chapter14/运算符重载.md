# 运算符重载

---

## 一元和二元成员函数

- **一元**：左侧运算对象传递给第一个参数
- **二元**：左侧运算对象传递给第一个参数，右侧运算对象传递给第二个参数

## 成员和非成员函数

对于一个运算符函数来说，要么是类的成员（隐式this对象），要么至少含有一个类类型的参数：

- **成员函数**：左侧运算符绑定到隐式的this指针上，因此成员运算符的显示参数比运算符的运算对象少一个
- **非成员函数**：至少包含一个类类型的对象
这一约定意味着当运算符作用于**内置类型**的运算对象时，我们无法改变运算符的含义（内置类型不是类对象）。
